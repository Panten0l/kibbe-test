name = "kibbe-proxy"
main = "worker.js"
compatibility_date = "2025-01-01"

# Адрес сайта, которому разрешено обращаться к Worker.
# Секреты (ANTHROPIC_API_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)
# задаются в дашборде Cloudflare → Settings → Variables and Secrets.
[vars]
ALLOWED_ORIGIN = "https://kibbe.omanpan.org"
